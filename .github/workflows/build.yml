name: Build & Push Docker Images

on:
  push:
    branches: [main]
    tags: ["v*"]

env:
  REGISTRY: ghcr.io
  IMAGE_PREFIX: ghcr.io/${{ github.repository_owner }}/umbra

jobs:
  build-nakama:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v5
        with:
          context: ./nakama
          push: true
          tags: ${{ env.IMAGE_PREFIX }}-nakama:${{ github.ref_name }}

  build-ai-director:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v5
        with:
          context: ./services/ai-director
          push: true
          tags: ${{ env.IMAGE_PREFIX }}-ai-director:${{ github.ref_name }}

  build-game-logic:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v5
        with:
          context: ./services/game-logic
          push: true
          tags: ${{ env.IMAGE_PREFIX }}-game-logic:${{ github.ref_name }}

  build-payment:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v5
        with:
          context: ./services/payment
          push: true
          tags: ${{ env.IMAGE_PREFIX }}-payment:${{ github.ref_name }}

  build-client:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - uses: docker/login-action@v3
        with:
          registry: ${{ env.REGISTRY }}
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/build-push-action@v5
        with:
          context: ./client
          push: true
          tags: ${{ env.IMAGE_PREFIX }}-client:${{ github.ref_name }}

  deploy:
    name: Deploy to Fly.io
    runs-on: ubuntu-latest
    needs: [build-nakama, build-ai-director, build-game-logic, build-payment, build-client]
    steps:
      - uses: actions/checkout@v4

      - name: Deploy ai-director
        uses: superfly/flyctl-action@1.5
        with:
          args: "deploy ./services/ai-director"
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

      - name: Deploy game-logic
        uses: superfly/flyctl-action@1.5
        with:
          args: "deploy ./services/game-logic"
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

      - name: Deploy payment
        uses: superfly/flyctl-action@1.5
        with:
          args: "deploy ./services/payment"
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

      - name: Deploy client
        uses: superfly/flyctl-action@1.5
        with:
          args: "deploy ./client"
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}

      - name: Deploy nakama
        uses: superfly/flyctl-action@1.5
        with:
          args: "deploy ./nakama"
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
